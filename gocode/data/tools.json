[
  {
    "name": "BashTool",
    "responsibility": "execute shell commands",
    "description": "Execute a shell command and return its stdout, stderr, and exit code. Use this for running tests, builds, git operations, or any CLI interaction.",
    "source_hint": "tools/bash",
    "input_schema": {
      "type": "object",
      "properties": {
        "command": { "type": "string", "description": "The shell command to execute" },
        "timeout_ms": { "type": "integer", "description": "Timeout in milliseconds (default: 30000)" }
      },
      "required": ["command"]
    }
  },
  {
    "name": "FileReadTool",
    "responsibility": "read file contents",
    "description": "Read the contents of a file at a given path. Supports optional line range selection for reading specific portions of large files.",
    "source_hint": "tools/file",
    "input_schema": {
      "type": "object",
      "properties": {
        "path": { "type": "string", "description": "Absolute or relative path to the file" },
        "start_line": { "type": "integer", "description": "First line to read (1-indexed, optional)" },
        "end_line": { "type": "integer", "description": "Last line to read (1-indexed, inclusive, optional)" }
      },
      "required": ["path"]
    }
  },
  {
    "name": "FileEditTool",
    "responsibility": "edit file contents",
    "description": "Edit a file by replacing an exact string match (old_text) with new content (new_text). The old_text must match exactly including whitespace and indentation.",
    "source_hint": "tools/file",
    "input_schema": {
      "type": "object",
      "properties": {
        "path": { "type": "string", "description": "Path to the file to edit" },
        "old_text": { "type": "string", "description": "Exact text to find and replace" },
        "new_text": { "type": "string", "description": "Replacement text" }
      },
      "required": ["path", "old_text", "new_text"]
    }
  },
  {
    "name": "FileWriteTool",
    "responsibility": "write new files",
    "description": "Create a new file or overwrite an existing file with the given content. Parent directories are created automatically.",
    "source_hint": "tools/file",
    "input_schema": {
      "type": "object",
      "properties": {
        "path": { "type": "string", "description": "Path to the file to create or overwrite" },
        "content": { "type": "string", "description": "Full content to write to the file" }
      },
      "required": ["path", "content"]
    }
  },
  {
    "name": "GlobTool",
    "responsibility": "search files by glob pattern",
    "description": "Find files matching a glob pattern. Returns a list of matching file paths. Useful for discovering project structure.",
    "source_hint": "tools/glob",
    "input_schema": {
      "type": "object",
      "properties": {
        "pattern": { "type": "string", "description": "Glob pattern (e.g. '**/*.go', 'src/**/*.ts')" },
        "path": { "type": "string", "description": "Base directory to search from (default: current directory)" }
      },
      "required": ["pattern"]
    }
  },
  {
    "name": "GrepTool",
    "responsibility": "search file contents",
    "description": "Search for a pattern in file contents recursively. Returns matching lines with file paths and line numbers.",
    "source_hint": "tools/grep",
    "input_schema": {
      "type": "object",
      "properties": {
        "pattern": { "type": "string", "description": "Search pattern (substring match)" },
        "path": { "type": "string", "description": "Directory or file to search (default: current directory)" },
        "include": { "type": "string", "description": "File glob filter (e.g. '*.go', '*.ts')" }
      },
      "required": ["pattern"]
    }
  },
  {
    "name": "ListDirectoryTool",
    "responsibility": "list directory contents",
    "description": "List the files and subdirectories in a given directory. Returns names, types (file/dir), and sizes.",
    "source_hint": "tools/directory",
    "input_schema": {
      "type": "object",
      "properties": {
        "path": { "type": "string", "description": "Directory path to list (default: current directory)" },
        "recursive": { "type": "boolean", "description": "Whether to list recursively (default: false)" }
      },
      "required": []
    }
  },
  {
    "name": "WebFetchTool",
    "responsibility": "fetch web page content",
    "description": "Fetch the content of a URL and return the response body as text. Useful for reading documentation, APIs, or web pages. Limited to 10KB response.",
    "source_hint": "tools/web",
    "input_schema": {
      "type": "object",
      "properties": {
        "url": { "type": "string", "description": "The URL to fetch" }
      },
      "required": ["url"]
    }
  },
  {
    "name": "WebSearchTool",
    "responsibility": "search the web",
    "description": "Search for information across Wikipedia, GitHub, Reddit, Hacker News, and StackOverflow in parallel. Returns articles, repos, discussions, and Q&A. No API key required.",
    "source_hint": "tools/web",
    "input_schema": {
      "type": "object",
      "properties": {
        "query": { "type": "string", "description": "The search query" }
      },
      "required": ["query"]
    }
  },
  {
    "name": "NotebookEditTool",
    "responsibility": "edit Jupyter notebook cells",
    "description": "Edit a Jupyter notebook (.ipynb) file by replacing an exact string match with new content. Works like FileEditTool but for notebook JSON files.",
    "source_hint": "tools/file",
    "input_schema": {
      "type": "object",
      "properties": {
        "path": { "type": "string", "description": "Path to the .ipynb notebook file" },
        "old_text": { "type": "string", "description": "Exact text to find and replace" },
        "new_text": { "type": "string", "description": "Replacement text" }
      },
      "required": ["path", "old_text", "new_text"]
    }
  },
  {
    "name": "orchestrator_delegate",
    "responsibility": "delegate task to specialist sub-agent",
    "description": "Delegate a task to a named specialist sub-agent and wait for the result. Available agents: coordinator (task decomposition), deep-worker (research and analysis), planner (structured planning), debugger (error diagnosis). Use this when a task requires specialized expertise or when you want to parallelize work.",
    "source_hint": "orchestrator",
    "input_schema": {
      "type": "object",
      "properties": {
        "agent_name": { "type": "string", "description": "Name of the sub-agent: coordinator, deep-worker, planner, or debugger" },
        "task": { "type": "string", "description": "The task description to send to the sub-agent" }
      },
      "required": ["agent_name", "task"]
    }
  },
  {
    "name": "orchestrator_delegate_bg",
    "responsibility": "delegate task to background sub-agent",
    "description": "Delegate a task to a named specialist sub-agent in the background. Returns immediately with a confirmation. The agent runs asynchronously. Use this for tasks that can run in parallel.",
    "source_hint": "orchestrator",
    "input_schema": {
      "type": "object",
      "properties": {
        "agent_name": { "type": "string", "description": "Name of the sub-agent: coordinator, deep-worker, planner, or debugger" },
        "task": { "type": "string", "description": "The task description to send to the sub-agent" }
      },
      "required": ["agent_name", "task"]
    }
  }
]
